version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - .:/rails
    env_file:
      - .env
    environment:
      - RAILS_ENV=development
      - DISABLE_ACTION_CONTROLLER_LOGGER=true
